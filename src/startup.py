"""The intial script to boot a start environment"""

print("... VOL boot (0, 0, 1) Kerbechet")

# keys = tuple(globals().keys())
# print(keys)
# for key in keys:
#     del globals()[key]

import bios
