# Single Identity

An "Identity" module should live in its own memory; accessible by its root method to Identify _who_ or _what_ has the current permissions state. This allows the open/close of the protected area.

